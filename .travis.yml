language: python
python:
 - "3.5.4"

install:
 - pip install -r requirements.txt
 # Submit coverage report to coveralls.io.
 - pip install coveralls

script:
 - python manage.py tests
 - coverage run --source=app python -m unittest

after_success:
 - coveralls
